import { pluginlist } from '../../../helpers/pluginlist';
import { module, test } from 'qunit';

module('Unit | Helper | pluginlist');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pluginlist(42);
  assert.ok(result);
});
