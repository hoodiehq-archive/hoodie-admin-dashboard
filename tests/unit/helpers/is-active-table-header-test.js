import { isActiveTableHeader } from '../../../helpers/is-active-table-header';
import { module, test } from 'qunit';

module('Unit | Helper | is active table header');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isActiveTableHeader(42);
  assert.ok(result);
});
