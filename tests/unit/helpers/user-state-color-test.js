import { userStateColor } from '../../../helpers/user-state-color';
import { module, test } from 'qunit';

module('Unit | Helper | user state color');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = userStateColor(42);
  assert.ok(result);
});
