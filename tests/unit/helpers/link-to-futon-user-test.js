import { linkToFutonUser } from '../../../helpers/link-to-futon-user';
import { module, test } from 'qunit';

module('Unit | Helper | link to futon user');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = linkToFutonUser(42);
  assert.ok(result);
});
