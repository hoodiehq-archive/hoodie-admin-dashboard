import { convertISOToTimestamp } from '../../../helpers/convert-iso-to-timestamp';
import { module, test } from 'qunit';

module('Unit | Helper | convert iso to timestamp');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertISOToTimestamp(42);
  assert.ok(result);
});
