import { pluginList } from '../../../helpers/plugin-list';
import { module, test } from 'qunit';

module('Unit | Helper | plugin list');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pluginList(42);
  assert.ok(result);
});
